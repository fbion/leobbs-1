#docker run -v c:/www:/var/www/html -p 80:80 -d docker-apache-perl /usr/sbin/apache2ctl -D FOREGROUND
version: "3.3"
services:
  leobbs:
      build: .
      ports:
         - "4321:80"
      restart: always
      networks:
        default:
networks:
  default: